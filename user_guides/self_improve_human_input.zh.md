# 人工介入式自我改进功能使用指南

> **Language**: [中文](self_improve_human_input.zh.md) | [English](self_improve_human_input.en.md)

## 概述

`self_improve_human_input.py` 是一个行为面试问题（BQ）答案的自我改进工具，它通过**人工介入循环**（Human-in-the-Loop）的方式，帮助您持续优化面试答案，直到达到"强烈推荐录用"（Strong Hire）的评级或达到最大迭代次数。

### 核心特点

1. **基于反馈迭代改进**：从已有的反馈文件中提取问题和初始答案，然后进行迭代优化
2. **智能提取探索性问题**：自动从反馈中提取需要进一步澄清的问题
3. **人工输入真实细节**：通过回答探索性问题，提供真实、具体的个人经历细节
4. **自动生成改进答案**：AI 将您的真实回答融入原始答案，生成更具体、更有说服力的改进版本
5. **持续评估优化**：每轮改进后重新评估，直到达到 Strong Hire 或达到最大迭代次数

---

## 工作原理

### 改进流程

```
初始答案
   ↓
提取反馈文件中的问题和答案
   ↓
评估当前答案 → 生成反馈和探索性问题
   ↓
判断是否达到 Strong Hire？
   ├─ 是 → 完成 ✅
   └─ 否 → 提取探索性问题
       ↓
   用户回答探索性问题（提供真实细节）
       ↓
   基于用户输入生成改进答案
       ↓
   重复评估...（最多 max_iterations 次）
```

### 关键组件

1. **`HumanInLoopImprove` 类**（位于 `advance/self_improve.py`）
   - 负责整个改进循环的逻辑
   - 管理迭代次数和状态
   - 协调评估、问题提取和答案改进

2. **探索性问题提取**
   - 从反馈中自动识别需要进一步澄清的问题
   - 通常围绕：Ownership（个人贡献）、Metrics（具体指标）、Impact（影响）、Reflection（反思）等维度

3. **答案改进机制**
   - 将您的真实回答与原始答案结合
   - 生成更具体、更有说服力的改进版本
   - 保持 STAR 方法的结构（Situation, Task, Action, Result）

---

## 使用方式

### 前置条件

1. **准备反馈文件**
   - 需要有一个已生成的反馈文件（`.md` 格式）
   - 反馈文件应包含：问题（Question）、答案（Answer）、反馈（Feedback）和评级（Rating）
   - 默认使用：`feedbacks/20251129-Leaning_No_Hire_4.md`

2. **安装依赖**
   - 确保已安装所有必需的 Python 包（见 `requirements.txt`）

### 方式一：交互式模式（推荐）

交互式模式会实时提示您回答问题，适合真实使用场景。

#### 运行步骤

1. **修改配置**（如需要）

   ```python
   improver = HumanInLoopImprove(
       feedback_file=FEEDBACK_FILE,  # 反馈文件路径
       level="Junior-Mid",            # 面试级别：Junior-Mid, Mid-Senior, Senior, etc.
       max_iterations=3,              # 最大迭代次数
   )
   ```

2. **运行程序**

   ```python
   async def main():
       improver = HumanInLoopImprove(
           feedback_file=FEEDBACK_FILE,
           level="Junior-Mid",
           max_iterations=3,
       )
       
       result = await improver.run_interactive()
       
       # 查看结果
       print(f"状态: {result['status']}")
       print(f"迭代次数: {result['iterations']}")
       print(f"最终答案:\n{result['final_answer']}")
   ```

3. **回答问题**

   程序会逐步提示：
   ```
   ============================================================
   Iteration 1
   ============================================================
   
   Evaluating answer...
   
   ----------------------------------------
   Please answer these probing questions to improve your answer:
   ----------------------------------------
   
   Q1: What was YOUR specific decision that changed the outcome?
   > [在此输入您的回答]
   
   Q2: What mistake did you make, and what would you do differently?
   > [在此输入您的回答]
   
   ...
   ```

4. **查看改进结果**

   每轮迭代后，程序会显示：
   - 改进后的答案
   - 当前评级
   - 是否达到 Strong Hire

#### 完整示例代码

```python
import asyncio
from pathlib import Path
from advance.self_improve import HumanInLoopImprove

FEEDBACK_FILE = Path("feedbacks") / "20251129-Leaning_No_Hire_4.md"

async def main():
    improver = HumanInLoopImprove(
        feedback_file=FEEDBACK_FILE,
        level="Junior-Mid",
        max_iterations=3,
    )
    
    print(f"原始问题: {await improver.question()}")
    print(f"\n原始答案:\n{await improver.answer()}")
    
    result = await improver.run_interactive()
    
    print("\n" + "=" * 80)
    print("最终结果")
    print("=" * 80)
    print(f"状态: {result['status']}")  # strong_hire 或 max_iterations
    print(f"迭代次数: {result['iterations']}")
    print(f"\n最终答案:\n{result['final_answer']}")

if __name__ == "__main__":
    asyncio.run(main())
```

### 方式二：预定义答案模式（测试/演示）

预定义答案模式使用预设的回答列表，适合测试和演示场景。

#### 使用场景

- 自动化测试
- 演示功能
- 批量处理多个答案

#### 运行步骤

1. **准备预定义答案**

   ```python
   PREDEFINED_PROBING_ANSWERS = [
       # 第一轮迭代的答案
       [
           "第一个探索性问题的回答...",
           "第二个探索性问题的回答...",
           "第三个探索性问题的回答...",
           # ... 更多回答
       ],
       # 第二轮迭代的答案（如果需要）
       [
           "第二轮第一个问题的回答...",
           # ...
       ],
   ]
   ```

   **注意**：答案的顺序必须与探索性问题的顺序一致。

2. **运行程序**

   ```python
   async def main():
       improver = HumanInLoopImprove(
           feedback_file=FEEDBACK_FILE,
           level="Junior-Mid",
           max_iterations=2,
       )
       
       result = await improver.run_with_predefined_answers(
           PREDEFINED_PROBING_ANSWERS
       )
       
       print(f"状态: {result['status']}")
       print(f"迭代次数: {result['iterations']}")
       print(f"\n最终答案:\n{result['final_answer']}")
   ```

3. **查看完整历史**

   结果字典包含完整的改进历史：
   - `answer_history`: 所有版本的答案（包括初始版本）
   - `feedback_history`: 每轮迭代的反馈和红 flag 检查

---

## 配置选项

### `HumanInLoopImprove` 初始化参数

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `feedback_file` | `Path \| None` | `None` | 反馈文件的路径 |
| `feedback_full_content` | `str \| None` | `None` | 反馈文件的完整内容（字符串形式） |
| `level` | `str` | `"Junior-Mid"` | 面试级别，影响评估标准。可选值：`"Junior-Mid"`, `"Mid-Senior"`, `"Senior"`, `"Staff"` 等 |
| `max_iterations` | `int` | `3` | 最大迭代次数。达到此次数后，即使未达到 Strong Hire 也会停止 |

### 返回值结构

#### `run_interactive()` 和 `run_with_predefined_answers()` 返回的字典：

```python
{
    "status": str,              # "strong_hire" 或 "max_iterations" 或 "completed"
    "final_answer": str,        # 最终改进后的答案
    "iterations": int,          # 实际迭代次数
    "answer_history": list,     # 答案历史：[初始答案, 第1次改进, 第2次改进, ...]
    "feedback_history": list,   # 反馈历史：[{"feedback": "...", "red_flag": "..."}, ...]
}
```

---

## 探索性问题类型

系统会自动从反馈中提取探索性问题。常见类型包括：

### 1. Ownership（个人贡献）
- "What was YOUR specific decision that changed the outcome?"
- "What role did you play in shaping the final decision?"
- "What was your personal contribution vs. the team's?"

### 2. Reflection（反思）
- "What mistake did you make, and what would you do differently?"
- "What would you do if faced with a similar situation again?"

### 3. Metrics（具体指标）
- "What specific metrics can you share about YOUR contribution?"
- "Can you quantify the impact with numbers?"

### 4. Impact（影响）
- "Can you describe a situation where your actions directly influenced the outcome?"
- "What was the tangible result of your intervention?"

### 5. Clarity（清晰度）
- "How did you ensure stakeholders were aligned on the data?"
- "What specific steps did you take to communicate effectively?"

---

## 最佳实践

### 1. 回答探索性问题的技巧

✅ **好的回答应该：**
- **具体明确**：提供具体的数字、时间、人物、决策
- **个人视角**：强调"我"做了什么，而非"我们"做了什么
- **真实可信**：基于真实的经历和细节
- **结构化**：使用 STAR 方法组织回答

✅ **示例（好的回答）：**
```
我的具体决策是提议 60/40 的资源分配比例，而不是大家都在争论的 50/50。
我通过分析两个项目的关键路径计算得出这个比例。我先向我的经理提出，
获得她的支持后，才在团队会议上作为具体提案提出，而不是再次开启争论。
```

❌ **避免：**
- 过于泛化的描述（"我做了很多工作"）
- 团队视角（"我们一起解决了问题"）
- 缺乏具体细节（"结果很好"）

### 2. 迭代策略

- **第1轮**：重点关注 Ownership 和 Impact，提供最核心的个人贡献细节
- **第2轮**：补充 Metrics 和 Clarity，添加具体数据和沟通细节
- **第3轮**：完善 Reflection，展示学习能力和自我改进意识

### 3. 判断何时停止

程序会在以下情况自动停止：
- ✅ 达到 Strong Hire 评级且无红 flag
- ⏱️ 达到最大迭代次数

**建议**：
- 如果连续2轮评级没有提升，考虑手动停止
- 如果答案已经非常详细和具体，可以接受当前的 Strong Hire

### 4. 文件管理

- 每轮迭代后，改进的答案会自动保存到 `feedbacks/` 目录
- 文件命名格式：`YYYYMMDD-Rating_N.md`
- 可以保留历史版本以便对比分析

---

## 完整示例

### 示例场景

假设您有一个关于"解决冲突"的 BQ 答案，初始评级为 "Leaning_No_Hire"。

#### 步骤 1：初始答案
```
原始答案描述了使用数据驱动方法解决资源分配冲突，但缺乏个人具体贡献的细节。
```

#### 步骤 2：第一轮改进

**探索性问题：**
1. "What was YOUR specific decision that changed the outcome?"
2. "What mistake did you make, and what would you do differently?"

**您的回答：**
1. "我提议了 60/40 的资源分配比例，并通过分析关键路径得出这个结论。"
2. "我的错误是没有在会议前私下与基础设施负责人沟通，导致他在会议上感到意外。"

**改进后的答案**会自动包含这些细节，使答案更加具体和可信。

#### 步骤 3：评估与继续

- 如果评级提升到 "Hire"，继续下一轮
- 如果达到 "Strong Hire"，完成 ✅

---

## 常见问题（FAQ）

### Q1: 反馈文件格式要求是什么？

反馈文件应包含以下部分：
- **Question**: 面试问题
- **Answer**: 原始答案
- **Feedback**: 详细反馈（包含探索性问题）
- **Rating**: 评级（如 Leaning_No_Hire, Hire, Strong Hire 等）

### Q2: 如何生成初始反馈文件？

可以使用 `examples/solve_conflict.py` 或其他示例脚本生成初始反馈。

### Q3: 能否跳过某些探索性问题？

在交互式模式中，您可以输入空字符串跳过某个问题，但这可能会影响改进效果。建议尽量回答所有问题。

### Q4: 如何处理多轮迭代？

每轮迭代都是独立的：
- 第一轮使用反馈文件中的反馈
- 后续轮次使用新评估生成的反馈
- 答案会持续改进和累积细节

### Q5: 如何调整评估严格程度？

通过 `level` 参数控制：
- `"Junior-Mid"`: 较宽松的标准
- `"Senior"`: 更严格的标准
- `"Staff"`: 最严格的标准

---

## 技术细节

### 依赖关系

- `advance.self_improve.HumanInLoopImprove`: 核心改进逻辑
- `interview_analyzer.InterviewAnalyzer`: AI 评估和分析
- `prompts.BQAnswer`, `prompts.BQQuestions`, `prompts.BQFeedback`: 提示词模板
- `utils.FeedbackParser`: 反馈解析和问题提取

### 异步处理

所有方法都是异步的（`async`），需要使用 `asyncio.run()` 运行。

---

## 相关文件

- **示例文件**: `examples/self_improve_human_input.py`
- **核心实现**: `advance/self_improve.py`
- **提示词模板**: `prompts.py`
- **工具函数**: `utils.py`
- **反馈文件示例**: `feedbacks/20251129-Leaning_No_Hire_4.md`

---

## 总结

`self_improve_human_input.py` 是一个强大的工具，通过结合 AI 评估和人工输入，帮助您创建更优秀、更具体的 BQ 答案。关键是要：

1. **提供真实的细节**：回答探索性问题时，尽可能具体和真实
2. **迭代改进**：不要期望一次就完美，多轮迭代会持续提升质量
3. **关注个人贡献**：强调"我"做了什么，而非团队做了什么
4. **保持耐心**：Strong Hire 可能需要 2-3 轮迭代才能达到

祝您面试顺利！🚀

